# Step 3: Draw a Triangle

The triangle is the GPU's Hello World. If you can draw one triangle, you understand
shaders, vertex buffers, and the GPU pipeline. Everything else — meshes, textures,
3D geometry — is more of the same, scaled up.

 - Read the build.sh file to see the generate/build/run commands.
 - 
