# This directory contains two independent CMake projects.
# Do not run cmake from here — use build.sh.
#
#   engine/   — standalone library project, builds and installs itself
#   snake/    — standalone game project, consumes Engine via find_package
#
# See build.sh for the two-step build process.
